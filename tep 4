warning: in the working copy of 'lib/recommendation/index.ts', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/app/api/analyze/route.ts b/app/api/analyze/route.ts[m
[1mindex 5eb7021..2b9ca30 100644[m
[1m--- a/app/api/analyze/route.ts[m
[1m+++ b/app/api/analyze/route.ts[m
[36m@@ -2,7 +2,7 @@[m [mimport { createHash } from 'crypto'[m
 import { cookies } from 'next/headers'[m
 import { NextRequest, NextResponse } from 'next/server'[m
 import { ipAddress, waitUntil } from '@vercel/functions'[m
[31m-import { streamRecommendCities, buildTeaserCities } from '@/lib/recommendation'[m
[32m+[m[32mimport { streamRecommendCities, buildTeaserCities, buildLockedScoreContinentStubs } from '@/lib/recommendation'[m
 import { resultCountForPlan, isPaidPlan, FREE_UNLOCKED_COUNT, FREE_DETAILED_COUNT, FREE_SEARCHES_PER_DAY, FREE_ANONYMOUS_SEARCHES_PER_MONTH } from '@/lib/plan'[m
 import { supabaseAdmin } from '@/lib/supabase-admin'[m
 import { createClient } from '@/lib/supabase/server'[m
[36m@@ -285,10 +285,18 @@[m [mexport async function POST(req: NextRequest) {[m
       try {[m
         const paid = isPaidPlan(plan)[m
         const resultCount = resultCountForPlan(plan)[m
[32m+[m[32m        const paywallV2 = isPaywallV2Enabled()[m
         // Free users only ever see ONE city in full, so we generate a small[m
         // detailed set instead of all 12 rich objects (the cause of the ~1 min[m
         // analyses). The locked grid is padded with cheap teasers below.[m
[31m-        const detailedCount = paid ? resultCount : FREE_DETAILED_COUNT[m
[32m+[m[32m        // Paywall-v2 free: LLM writes engine #4–#12 (9 full cities); #1–#3 are[m
[32m+[m[32m        // score+continent stubs. Flag off keeps today's 3 + teasers.[m
[32m+[m[32m        const detailedCount = paid[m
[32m+[m[32m          ? resultCount[m
[32m+[m[32m          : paywallV2[m
[32m+[m[32m            ? Math.max(1, resultCount - FREE_UNLOCKED_COUNT)[m
[32m+[m[32m            : FREE_DETAILED_COUNT[m
[32m+[m[32m        const rankOffset = !paid && paywallV2 ? FREE_UNLOCKED_COUNT : 0[m
 [m
         send({ type: 'limits', maxCities: resultCount })[m
         send({ type: 'status', text: 'Scoring cities and writing your personalized insights…' })[m
[36m@@ -296,10 +304,13 @@[m [mexport async function POST(req: NextRequest) {[m
         // Stream the #1 match unlocked the moment it parses so the free user[m
         // sees their top card within a few seconds; later matches stream as[m
         // locked teasers. Paid users get every city unlocked progressively.[m
[32m+[m[32m        // Paywall-v2 free: onCity only receives engine #4–#12 (rankOffset=3);[m
[32m+[m[32m        // those 9 are the free full cities. True #1–#3 are locked stubs on done.[m
         let emitted = 0[m
         const cities = await streamRecommendCities(request, detailedCount, {[m
[32m+[m[32m          rankOffset,[m
           onCity(city) {[m
[31m-            const unlock = paid || emitted < FREE_UNLOCKED_COUNT[m
[32m+[m[32m            const unlock = paid || paywallV2 || emitted < FREE_UNLOCKED_COUNT[m
             send({[m
               type: 'city',[m
               city: unlock ? { ...city, locked: false } : sanitizeLockedCity(city),[m
[36m@@ -311,6 +322,10 @@[m [mexport async function POST(req: NextRequest) {[m
         let clientCities: CityResult[][m
         if (paid) {[m
           clientCities = cities.map((city) => ({ ...city, locked: false }))[m
[32m+[m[32m        } else if (paywallV2) {[m
[32m+[m[32m          const stubs = buildLockedScoreContinentStubs(request, FREE_UNLOCKED_COUNT)[m
[32m+[m[32m          const unlocked = cities.map((city) => ({ ...city, locked: false }))[m
[32m+[m[32m          clientCities = [...stubs, ...unlocked][m
         } else {[m
           // Keep the first generated match as the unlocked #1 (matches what we[m
           // streamed, so the top card never flashes), then sanitize the rest[m
[36m@@ -342,7 +357,6 @@[m [mexport async function POST(req: NextRequest) {[m
             .eq('id', userId)[m
         }[m
 [m
[31m-        const paywallV2 = isPaywallV2Enabled()[m
         const searchId = paywallV2 ? createAnalyzeSearchId() : null[m
         if (paywallV2 && searchId) {[m
           send({ type: 'done', cities: clientCities, searchId })[m
[1mdiff --git a/lib/recommendation/index.ts b/lib/recommendation/index.ts[m
[1mindex fb3346c..3420fc8 100644[m
[1m--- a/lib/recommendation/index.ts[m
[1m+++ b/lib/recommendation/index.ts[m
[36m@@ -480,6 +480,39 @@[m [mexport function unsanitizedCitiesForPersist([m
   })[m
 }[m
 [m
[32m+[m[32m/**[m
[32m+[m[32m * Locked paywall-v2 stubs for engine ranks #1–#3: score + continent only.[m
[32m+[m[32m * Zero-width names keep Results keys unique without revealing identity.[m
[32m+[m[32m */[m
[32m+[m[32mexport function buildLockedScoreContinentStubs([m
[32m+[m[32m  body: AnalyzeRequest,[m
[32m+[m[32m  count: number,[m
[32m+[m[32m): CityResult[] {[m
[32m+[m[32m  return rankSurvivorsForUser(body, count).map((ranked, i) => {[m
[32m+[m[32m    const meta = metaFor(ranked.city)[m
[32m+[m[32m    return {[m
[32m+[m[32m      name: "\u200B".repeat(i + 1),[m
[32m+[m[32m      country: "",[m
[32m+[m[32m      continent: meta.continent,[m
[32m+[m[32m      flag: "",[m
[32m+[m[32m      score: Math.round(ranked.score!),[m
[32m+[m[32m      taxRate: 0,[m
[32m+[m[32m      monthlyRent: 0,[m
[32m+[m[32m      monthlyCost: 0,[m
[32m+[m[32m      takeHomeMonthly: 0,[m
[32m+[m[32m      monthlySavings: 0,[m
[32m+[m[32m      pros: [],[m
[32m+[m[32m      cons: [],[m
[32m+[m[32m      tags: [],[m
[32m+[m[32m      visa: "",[m
[32m+[m[32m      healthcare: "",[m
[32m+[m[32m      scores: { tax: 0, housing: 0, climate: 0, health: 0, stability: 0, safety: 0 },[m
[32m+[m[32m      aiInsight: "",[m
[32m+[m[32m      locked: true,[m
[32m+[m[32m    }[m
[32m+[m[32m  })[m
[32m+[m[32m}[m
[32m+[m
 function mergeNarrative(base: CityResult, partial: Partial<CityResult>): CityResult {[m
   return {[m
     ...base,[m
[36m@@ -851,6 +884,8 @@[m [mfunction buildNarrativeRequestBody([m
 export type RecommendStreamHandlers = {[m
   onDelta?: (text: string) => void[m
   onCity?: (city: CityResult) => void[m
[32m+[m[32m  /** Skip this many top-ranked cities before LLM generation. Paywall-v2 free path uses 3 → engine #4–#12. */[m
[32m+[m[32m  rankOffset?: number[m
 }[m
 [m
 function emitNarrativesFromBuffer([m
[36m@@ -937,13 +972,14 @@[m [mexport async function streamRecommendCities([m
   handlers: RecommendStreamHandlers = {}[m
 ): Promise<CityResult[]> {[m
   const resultCount = Math.max(1, Math.min(PRO_RESULT_COUNT, Math.round(count)))[m
[32m+[m[32m  const offset = Math.max(0, Math.min(PRO_RESULT_COUNT - 1, Math.round(handlers.rankOffset ?? 0)))[m
   const apiKey = process.env.OPENAI_API_KEY[m
   if (!apiKey) {[m
     throw new Error("OPENAI_API_KEY is not configured")[m
   }[m
 [m
   const priorities = normPrioritiesFromBody(body)[m
[31m-  const ranked = rankSurvivorsForUser(body, resultCount)[m
[32m+[m[32m  const ranked = rankSurvivorsForUser(body, offset + resultCount).slice(offset)[m
   if (ranked.length === 0) {[m
     throw new Error("No cities matched your budget and priority filters")[m
   }[m
